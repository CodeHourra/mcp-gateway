# MCP Gateway

个人 macOS MCP 管理器，使用 Wails v3 + Vue 3，复用固定版本的 MCPProxy Go。提供服务和认证管理、工具目录与调用、配置导入/备份、五类客户端接入及原生菜单栏。

当前为开发中的本地应用。受控协议和关键数据检查已取得结果，真实提供方、真实客户端和原生交互的完成情况以 [验收报告](docs/acceptance.md) 为准；未全部通过前不作为完成版本。

## DMG 安装与实机测试

本地安装包为 `bin/MCP-Gateway-0.1.8-arm64.dmg`，适用于 Apple Silicon / macOS 15 或以上。双击挂载，将 `MCP Gateway.app` 拖入同窗口的 `Applications`，弹出磁盘映像后从「应用程序」打开。

DMG 不做开发者签名或公证，应用保留无需证书的本机 ad-hoc 签名。若 macOS 提示无法验证开发者，按[安装与测试说明](docs/install-and-verify.md)在系统设置中允许这一个应用。安装包内也附有纯文本说明。

先完成启动、菜单、关窗和退出测试，再逐项接入真实服务和客户端。需要用户参与的项目保持待验收，反馈后继续修复和复验。

## 本地构建

需要当前 Apple Silicon Mac、macOS 15 或以上、Xcode Command Line Tools、Go、Node.js/npm、Git 和 Python 3。构建会使用模块声明的 Go 工具链，不修改用户全局 Go 配置。

```sh
./scripts/build.sh
./scripts/package-dmg.sh
open 'bin/MCP-Gateway-0.1.8-arm64.dmg'
```

脚本固定 Wails `v3.0.0-beta.16`、MCPProxy `v0.65.0`（commit `308a81272844df896b616b886295305d97f90f8d`），应用 [核心补丁](patches/mcpproxy/)，生成前端绑定，执行检查，打包并做本机 ad-hoc 签名。该签名不等于开发者签名或 Apple 公证。应用不会替换 `/Applications` 中任何内容。

`package-dmg.sh` 使用已构建的应用，通过系统 `hdiutil` 生成并校验压缩 DMG，旁置 `.sha256` 校验文件。它只打包应用、安装指引和 Applications 快捷方式，不包括开发环境或用户数据，也不执行安装。已有应用时可单独运行此打包脚本。

版本与边界见 [核心集成说明](docs/core-integration.md)、[客户端适配说明](docs/client-adapters.md)。运行时不需要启动 npm/Vite，也不需要单独安装核心；stdio 上游声明的命令仍需在本机存在。

## 使用

1. 打开应用，在服务页添加 stdio、HTTP 或 SSE 服务，填写连接信息及认证。命令与工作目录建议使用绝对路径，避免 Finder 启动与终端 PATH 不同。
2. 服务页默认收起工具，“展开工具”查看分层目录，“配置详情”管理连接与认证；也可在工具页检查真实目录、参数 schema，按需启用或停用工具，并查看完整调用结果。离线服务的目录标记为缓存或不可用。
3. 打开导入页时，应用会只读扫描 OMP、Claude Code、Cursor、CodeBuddy 和 Codex 的已知用户配置路径；选择检测到的来源后仍需预览、选择并确认，应用不会自动导入。也可手动选择或粘贴 JSON、JSONC、TOML。不同凭证、参数、环境变量和工作目录视为不同连接；应用自己生成的 `mcp-gateway connect` 客户端条目会被阻止，避免把网关接回自身。
4. 在 Agent 接入页选择 OMP、Claude Code、Cursor、CodeBuddy 或 Codex，预览目标配置变更后再写入。应用保留已有连接及无关配置，不会自动迁移全部客户端。

启动时显示管理窗口和 Dock 图标；关闭主窗口后隐藏 Dock 图标，网关继续在菜单栏运行。使用菜单栏重新打开；退出会先暂停新调用并等待已开始的调用结束，也可以选择立即停止。只管理本应用启动的核心和上游进程。

默认监听 `127.0.0.1:17840`。渐进模式使用 `/mcp/call`，普通聚合使用 `/mcp/all`。两者都要求独立客户端凭证；管理接口使用另一套凭证。接入适配器写入随应用附带的 `connect` 命令，从钥匙串读取客户端凭证，每个连接进程持有独立的网关会话。

## 数据、认证与恢复

应用数据默认位于 `~/Library/Application Support/MCP Gateway/`，包含 `settings.json`、`services.json`、`core.json`、加密 OAuth 数据和 `backups/`。目录限制为当前用户访问；上游静态凭证和本机接口凭证保存在 macOS 钥匙串。普通配置使用凭证引用，诊断导出只包含允许的状态字段。

OAuth 支持开始登录、取消、刷新、重新登录和清除本地授权。清除本地授权不会替用户撤销提供方网站上的授权。受控 OAuth 测试与真实提供方实测分别记录。供应方要求的 Client ID 与 Scopes 在认证页填写。

保存服务、导入、写入客户端配置和恢复前创建备份。设置页的「配置备份」可恢复配置；恢复前会等待网关调用结束并再次备份当前状态。网关配置备份保留凭证引用，不包含钥匙串的独立副本，因此复制备份到另一台 Mac 后仍需重新填写凭证或登录。客户端原文件按原始字节备份，若原文件含内联凭证，备份也会保留；它受本机目录/文件权限保护，不属于可公开分享的脱敏导出。删除本地 OAuth 授权无法通过配置备份恢复。

监听端口冲突时不会连接或终止占用端口的其他进程。设置页可改用空闲的本机端口，然后重试启动；即使核心未能启动也可以修复监听配置。意外核心退出会显示错误，可从窗口或菜单重试。

日志保留天数在设置页配置；变更后会等待当前调用结束并重启核心，使新策略生效。活动记录在启动和定期清理时按时间删除；文件日志按 10 MB 轮转，保留天数约束归档文件，不逐行删除当前日志。诊断导出不会包含工具参数/结果、命令、连接 URL、Header、环境变量或 OAuth 凭证。当前开发版尚无本应用的发布源，检查更新会明确显示无法判断，而不会假报“已是最新”。

活动页的「导出诊断」会将 JSON 文件保存到应用数据目录下的 `diagnostics/`，显示完整路径并尝试打开该目录。文件名包含时间戳，文件仅当前用户可读写；可从这里取用或分享脱敏诊断。

## 开发与验证

```sh
go test -race ./...
go vet ./...
npm --prefix frontend run build
npm --prefix frontend run check
```

协议检查需要允许本机回环端口监听。`.cache/`、`bin/`、`artifacts/` 与前端生成文件不纳入源码版本管理。可用 `MCP_GATEWAY_DATA_DIR` 指定隔离测试数据目录，`MCP_GATEWAY_CORE` 指定明确的核心可执行文件；不会自动接管其他 MCPProxy 实例。

原生测试可临时使用 `-tags production,mcp` 构建，并通过 Wails 内置的本机 MCP 检查窗口和 DOM。交付构建只使用 `production`，不包含该测试控制接口。浏览器独立打开前端时没有桌面绑定，会明确显示未连接，不使用伪造数据。

项目范围、分工和全部待验收项见 [项目目标](docs/project-goal.md)、[实施记录](docs/execution-plan.md) 和 [验收报告](docs/acceptance.md)。

选择 MCP 服务后，可直接在详情顶部点击「删除服务」。确认后会先备份网关配置，再删除服务；原始导入文件不受影响。

命令查找会在保留现有 PATH 顺序的基础上补齐已发现的用户工具目录（如 `~/.local/bin`）。连接失败时显示中文提示，可展开「错误详情」查看未截断的脱敏错误。

首页先读取本地已保存的服务，连接状态和工具目录在后台更新；启动时不会先显示成空服务列表。状态为「等待连接状态」时不表示连接成功。
