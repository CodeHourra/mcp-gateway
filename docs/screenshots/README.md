# 运行截图来源

采集日期：2026-09-08。应用版本：0.1.10 开发版。平台：macOS / Apple Silicon。

三张 PNG 均由 macOS `screencapture` 按窗口编号直接采集，保留原生窗口与实际 WKWebView 画面，未使用生成式图片、HTML 仿制界面或合成调用结果。

截图使用[0.1.10 原生验收](../../tests/acceptance/gateway-debug-ui-0.1.10-2026-09-07.json)对应的隔离构建。采集前已核对应用与核心二进制 SHA-256，均与该报告一致。该构建的业务源码和前端与 0.1.10 相同，仅应用身份和窗口标题用于隔离；截图时窗口标题设为「MCP Gateway · 本地演示」。

演示实例使用独立数据目录，核心仅监听 `127.0.0.1:19260`。`demo-echo` 运行仓库已有的[本地 stdio 协议探针](../../patches/mcpproxy/probes/protocol_probe.py)，`optional-echo` 为停用的演示配置。没有加载个人服务配置、调用真实远程提供方或修改客户端接入配置。

| 文件 | 尺寸 | 实际展示内容 |
| --- | --- | --- |
| [01-services.png](01-services.png) | 2360 × 1640 | 浅色服务列表；展开可用服务的工具目录，同时展示停用服务 |
| [02-tool-call.png](02-tool-call.png) | 2360 × 2034 | 工具参数与真实调用结果；页面滚动到结果区域，完整显示输入和返回内容 |
| [03-settings-dark.png](03-settings-dark.png) | 2360 × 1640 | 深色设置页；主题、发现模式、本机地址和运行选项 |

工具调用通过应用实际的「执行调用」操作完成，输入为 `{"text":"Hello, MCP Gateway!"}`；已检查返回内容与 `structuredContent.echo` 均包含该文本，且结果不为错误。截图中的计数、进程号和本机端口均为此次演示实例的运行值。

这些截图展示界面与本地工具调用，不替代真实客户端、远程服务或系统行为的完整验收；剩余范围见[验收报告](../acceptance.md)。
