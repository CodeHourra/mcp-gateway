# Git 历史与审阅顺序

2026-09-07 首次建立提交历史。此前已有 Git 仓库，但 main 没有任何提交。以下提交均使用实际补录时间，没有回填日期，也不冒充开发当时的原始提交。

| 提交 | 内容 | 审阅边界 |
| --- | --- | --- |
| `d7eb492` | 0.1.2 源码基线 | 包含 0.1.0–0.1.2 已完成工作；更早的完整源码快照不足，不拆造更早版本。恢复的 10 个导入/UI 文件哈希与 0.1.2 独立报告逐项一致。 |
| `49e072c` | 0.1.3 删除服务入口 | 顶部入口、删除确认固定目标与对应回归。 |
| `82242d8` | 0.1.4 连接体验 | 用户 PATH 补齐、中文错误与完整详情，以及当时的始终菜单栏模式。 |
| `890e821` | 验收与交付材料归档 | 保留各报告原始版本、哈希和待验收边界。 |
| 本文件所在 0.1.5 提交 | 图标留白与 Dock 生命周期 | 修正为启动展示管理器/Dock，关窗隐藏 Dock，重开恢复；附本轮验证。 |

使用 `git log --oneline --reverse` 查看顺序，`git show 49e072c` 或 `git diff d7eb492..82242d8` 审阅改动。缓存、运行配置、密钥、依赖目录和 DMG/app 构建产物不纳入 Git；版本与产物哈希保留在 tests/acceptance 的构建清单中。本轮仅提交到本地，没有推送远端。

0.1.2 校验依据：[导入/UI 独立报告](../tests/acceptance/import-scan-review-0.1.2-2026-09-07.json)。更早的验收记录用于追溯行为证据，不代表具备对应源码的完整 Git 快照。
