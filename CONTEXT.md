# 本地 MCP 管理

个人在一台电脑上集中管理多个 MCP 服务，让多个 agent 通过统一入口发现和使用工具。

## Language

**MCP 服务**：提供一组 MCP 能力的本地程序或远程服务。
_Avoid_: 把服务和其中的单个工具混称为 MCP。

**工具**：MCP 服务暴露的一个可调用能力，包含参数定义。

**网关**：面向 agent 提供统一 MCP 接入点、连接多个 MCP 服务的运行程序。
_Avoid_: 把仅编辑配置的管理界面也称为网关。

**管理器**：用户查看、配置和管理网关及其 MCP 服务的桌面界面。

**渐进发现**：agent 先获得少量发现入口，再按任务读取所需工具定义并调用工具的方式。
_Avoid_: 将减少工具定义的初始暴露，与延迟启动服务、执行代码混为一谈。

## Relationships

- 一个**网关**连接多个 **MCP 服务**，一个 **MCP 服务**提供多个**工具**。
- 多个 agent 可以接入同一个**网关**。
- **管理器**管理**网关**；关闭主窗口后继续常驻，主动退出应用时停止本应用拥有的网关运行进程。
- **渐进发现**改变 agent 获取工具信息的方式，不直接决定上游连接和进程数量。

## Example dialogue

以下是说明术语的假设场景，不是已确认的产品决定。

> **开发者：** 用户关闭管理器后，网关是否也停止？
> **使用者：** 这要单独决定；关掉窗口、退出管理器和停止网关不能含糊地写成同一个“关闭”。

## Flagged ambiguities

- “支持 OAuth”可能指 agent 访问网关的授权，也可能指网关访问上游 MCP 的授权；两者分开描述。需求重点包括后者的登录、保存和刷新。
- “菜单栏”本轮按 macOS 右上角状态项及其菜单设计；不同于应用的“文件 / 编辑 / 窗口”主菜单。
- “退出”按已批准项目目标停止受管网关；不停止外部进程。正在执行的调用需先等待，或由用户明确选择立即停止。
